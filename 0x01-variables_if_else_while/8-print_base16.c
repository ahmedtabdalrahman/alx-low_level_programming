#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints all single digits num of base 16 using putchar
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num;
	char c;

	for (num = 48; num < 58; n++)
	{
		putchar(n);
	}
	for (c = 'a'; c <= 'f'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
