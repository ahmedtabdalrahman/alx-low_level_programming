#include "main.h"

/**
 * rev_string - function that reverses a string
 *
 * @s: function input
 */

void rev_string(char *s)
{
	int cnt, ;

	for (cnt = 0; s[cnt] != 0; cnt++)
		c = s[i];

	for (i = 0; i <= cnt , i++)
		d[i] = c[cnt - i];
		
	s = d;
}
