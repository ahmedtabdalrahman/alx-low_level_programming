#include "main.h"

/**
 * print_rev - prints a string, in reverse
 *
 * @s: function input
 */

void print_rev(char *s)
{
	int cnt;

	while (s[cnt])
		i++;

	while (cnt--)
		_putchar(s[cnt]);
	_putchar('\n');
}
